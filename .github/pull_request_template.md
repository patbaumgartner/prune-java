## What and why

<!-- What changes, and what problem it solves. The diff already says how. -->

## Verification

- [ ] `./mvnw install` and `cd prune-gradle-plugin && ./gradlew build` pass
- [ ] A test fails without this change
- [ ] Rule change: the README table and each sample's `expected-findings.txt` changed in this PR (or n/a)
- [ ] Contract change (exit codes, output, plugin parameters, LSP): README and AGENTS.md updated (or n/a)
